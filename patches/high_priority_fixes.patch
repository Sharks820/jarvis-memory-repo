diff --git a/engine/src/jarvis_engine/desktop_widget.py b/engine/src/jarvis_engine/desktop_widget.py
--- a/engine/src/jarvis_engine/desktop_widget.py
+++ b/engine/src/jarvis_engine/desktop_widget.py
@@ -13,6 +13,7 @@ from typing import Any
 from urllib.error import HTTPError, URLError
 from urllib.request import Request, urlopen
 
+from concurrent.futures import ThreadPoolExecutor
 import tkinter as tk
 
 
@@ -231,6 +232,7 @@ class JarvisDesktopWidget(tk.Tk):
         self.stop_event = threading.Event()
         self.online = False
         self._pulse_phase = 0.0
+        self._executor = ThreadPoolExecutor(max_workers=4, thread_name_prefix="jarvis_widget")
         self.launcher_win: tk.Toplevel | None = None
         self._drag_offset_x = 0
         self._drag_offset_y = 0
@@ -257,6 +259,7 @@ class JarvisDesktopWidget(tk.Tk):
 
     def _shutdown(self) -> None:
         self.stop_event.set()
+        self._executor.shutdown(wait=False)
         if self.launcher_win is not None:
             try:
                 self.launcher_win.destroy()
@@ -636,7 +639,12 @@ class JarvisDesktopWidget(tk.Tk):
         self._thread(worker)
 
     def _thread(self, fn) -> None:  # type: ignore[no-untyped-def]
-        threading.Thread(target=fn, daemon=True).start()
+        self._executor.submit(self._wrapped_thread_fn, fn)
+
+    def _wrapped_thread_fn(self, fn) -> None:  # type: ignore[no-untyped-def]
+        try:
+            fn()
+        except Exception as exc:
+            self._log_async(f"Background task error: {exc}")
 
     def _send_command_async(self) -> None:
         text = self.command_text.get("1.0", tk.END).strip()
@@ -727,14 +735,23 @@ class JarvisDesktopWidget(tk.Tk):
         self._thread(worker)
 
     def _hotword_loop(self) -> None:
+        consecutive_errors = 0
         while self.hotword_var.get() and (not self.stop_event.is_set()):
             try:
                 heard = _detect_hotword_once(keyword="jarvis", timeout_s=2)
+                consecutive_errors = 0  # Reset on success
                 if heard:
                     self.after(0, self._show_panel)
                     self._log_async("Wake word detected.")
                     self.after(0, self._dictate_async)
-            except Exception:
-                pass
+            except Exception as exc:
+                consecutive_errors += 1
+                if consecutive_errors <= 3 or consecutive_errors % 10 == 0:
+                    self._log_async(f"Hotword detection error (attempt {consecutive_errors}): {exc}")
+                # Brief backoff on repeated errors
+                if consecutive_errors > 5:
+                    time.sleep(1.0)
+                continue
             for _ in range(6):
                 if self.stop_event.is_set() or (not self.hotword_var.get()):
                     return
@@ -155,7 +172,8 @@ def _http_error_details(exc: HTTPError) -> str:
     try:
-        raw = exc.read().decode("utf-8", errors="replace").strip()
+        with exc:
+            raw = exc.read().decode("utf-8", errors="replace").strip()
     except Exception:
         raw = ""
     if raw:
@@ -166,6 +184,7 @@ def _http_error_details(exc: HTTPError) -> str:
 
 def _voice_dictate_once(timeout_s: int = 8) -> str:
+    timeout_int = int(max(2, min(300, timeout_s)))  # Strict bounds
     script = (
         "Add-Type -AssemblyName System.Speech; "
         "$r = New-Object System.Speech.Recognition.SpeechRecognitionEngine; "
         "$r.SetInputToDefaultAudioDevice(); "
         "$r.LoadGrammar((New-Object System.Speech.Recognition.DictationGrammar)); "
-        f"$res = $r.Recognize([TimeSpan]::FromSeconds({int(max(2, timeout_s))})); "
+        f"$res = $r.Recognize([TimeSpan]::FromSeconds({timeout_int})); "
         "if ($res) { $res.Text }"
     )
 
@@ -197,6 +216,7 @@ def _voice_dictate_once(timeout_s: int = 8) -> str:
 
 def _detect_hotword_once(keyword: str = "jarvis", timeout_s: int = 2) -> bool:
     keyword = keyword.strip().lower()[:40] or "jarvis"
+    timeout_int = int(max(1, min(60, timeout_s)))  # Strict bounds
     if not re.fullmatch(r"[a-z0-9 ]{1,40}", keyword):
         return False
     script = (
@@ -208,7 +228,7 @@ def _detect_hotword_once(keyword: str = "jarvis", timeout_s: int = 2) -> bool:
         f"$choices.Add('{keyword}'); "
         "$grammar = New-Object System.Speech.Recognition.Grammar((New-Object System.Speech.Recognition.GrammarBuilder($choices))); "
         "$r.LoadGrammar($grammar); "
-        f"$res = $r.Recognize([TimeSpan]::FromSeconds({int(max(1, timeout_s))})); "
+        f"$res = $r.Recognize([TimeSpan]::FromSeconds({timeout_int})); "
         "if ($res) { $res.Text }"
     )
     proc = subprocess.run(["powershell", "-NoProfile", "-Command", script], capture_output=True, text=True, timeout=15)

diff --git a/engine/src/jarvis_engine/voice.py b/engine/src/jarvis_engine/voice.py
--- a/engine/src/jarvis_engine/voice.py
+++ b/engine/src/jarvis_engine/voice.py
@@ -161,8 +161,16 @@ def _speak_text_edge(
     voice = _choose_edge_voice(profile=profile, custom_pattern=custom_voice_pattern)
     if not voice:
         raise RuntimeError("No edge-tts voices found.")
 
+    # Path traversal protection
     if output_wav:
-        out_path = str(Path(output_wav).resolve())
+        out_path_obj = Path(output_wav).resolve()
+        temp_root = Path(tempfile.gettempdir()).resolve()
+        # Allow only temp directory or explicit output directories
+        try:
+            out_path_obj.relative_to(temp_root)
+        except ValueError:
+            raise RuntimeError(f"Output path must be within temp directory: {output_wav}")
+        out_path = str(out_path_obj)
     else:
         out_path = str(Path(tempfile.gettempdir()) / "jarvis_voice_edge.mp3")
     try:
@@ -240,7 +248,15 @@ def speak_text(
     if not voice:
         raise RuntimeError("No Windows voices found.")
 
     rate = max(-10, min(10, rate))
-    out_path = str(Path(output_wav).resolve()) if output_wav else ""
+    out_path = ""
+    if output_wav:
+        out_path_obj = Path(output_wav).resolve()
+        temp_root = Path(tempfile.gettempdir()).resolve()
+        try:
+            out_path_obj.relative_to(temp_root)
+            out_path = str(out_path_obj)
+        except ValueError:
+            raise RuntimeError(f"Output path must be within temp directory: {output_wav}")
     env = os.environ.copy()
     env["JARVIS_VOICE_TEXT"] = text
     env["JARVIS_VOICE_NAME"] = voice
