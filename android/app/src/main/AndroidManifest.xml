<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Voice -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Call screening -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Calendar (scheduling cue extraction creates events) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Audio settings (context-aware ringer mode changes) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Boot receiver for auto-start -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact alarms for medication dose reminders -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Location (commute intelligence, parking memory) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Bluetooth (parking memory: detect car BT disconnect) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <!-- Contacts (relationship memory: resolve contact names) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Camera (document scanner) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".JarvisApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Jarvis"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DynamicColors.DayNight">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground sync service (fully implemented in Plan 10-02) -->
        <service
            android:name=".service.JarvisService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Call screening service (intercepts incoming calls for spam detection) -->
        <service
            android:name=".feature.callscreen.JarvisCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Notification listener for scheduling cue extraction from SMS/email -->
        <service
            android:name=".feature.scheduling.JarvisNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Dose alarm receiver for medication reminders -->
        <receiver
            android:name=".feature.prescription.DoseAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jarvis.assistant.DOSE_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Dose action receiver for Taken/Skip notification buttons -->
        <receiver
            android:name=".feature.prescription.DoseActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jarvis.assistant.DOSE_TAKEN" />
                <action android:name="com.jarvis.assistant.DOSE_SKIPPED" />
            </intent-filter>
        </receiver>

        <!-- Nudge action receiver for Done/Dismiss habit nudge buttons -->
        <receiver
            android:name=".feature.habit.NudgeActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jarvis.assistant.NUDGE_ACTED" />
                <action android:name="com.jarvis.assistant.NUDGE_DISMISSED" />
            </intent-filter>
        </receiver>

        <!-- Call state receiver for pre/post-call context -->
        <receiver
            android:name=".feature.social.CallStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Post-call logging receiver for inline note capture -->
        <receiver
            android:name=".feature.social.PostCallLogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jarvis.assistant.POST_CALL_LOG" />
                <action android:name="com.jarvis.assistant.POST_CALL_SKIP" />
            </intent-filter>
        </receiver>

        <!-- Disable default WorkManager initializer (we use HiltWorkerFactory) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

    </application>

</manifest>
