---
phase: 04-connectors-and-daily-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - engine/src/jarvis_engine/ops_sync.py
  - engine/src/jarvis_engine/life_ops.py
  - engine/src/jarvis_engine/handlers/ops_handlers.py
  - engine/tests/test_email_briefing.py
autonomous: true
requirements: [CONN-02, CONN-04]

must_haves:
  truths:
    - "Email connector fetches real unread emails via IMAP with sender, subject, date, and importance fields"
    - "Email triage classifies importance using sender + subject keyword multi-signal approach"
    - "IMAP connection uses timeout and read-only mode for safety"
    - "Daily briefing generates an LLM-powered narrative via ModelGateway routed to local Ollama"
    - "Daily briefing includes calendar events, emails, tasks, medications, and memory context woven into a coherent narrative"
    - "When LLM is unavailable, briefing falls back to the existing deterministic build_daily_brief()"
    - "Briefing data is condensed before LLM prompt to stay under 2000 tokens"
  artifacts:
    - path: "engine/src/jarvis_engine/ops_sync.py"
      provides: "Enhanced IMAP email reader with sender/date headers and multi-signal triage"
      contains: "_triage_email"
    - path: "engine/src/jarvis_engine/life_ops.py"
      provides: "Two-stage daily briefing: deterministic assembly + LLM narrative synthesis"
      contains: "build_narrative_brief"
    - path: "engine/src/jarvis_engine/handlers/ops_handlers.py"
      provides: "OpsBriefHandler updated to optionally use narrative generation"
      contains: "build_narrative_brief"
    - path: "engine/tests/test_email_briefing.py"
      provides: "Tests for email triage, narrative briefing, fallback behavior"
      min_lines: 80
  key_links:
    - from: "engine/src/jarvis_engine/life_ops.py"
      to: "engine/src/jarvis_engine/gateway/models.py"
      via: "ModelGateway.complete() for narrative generation"
      pattern: "gateway\\.complete|ModelGateway"
    - from: "engine/src/jarvis_engine/handlers/ops_handlers.py"
      to: "engine/src/jarvis_engine/life_ops.py"
      via: "OpsBriefHandler calls build_narrative_brief()"
      pattern: "build_narrative_brief"
    - from: "engine/src/jarvis_engine/life_ops.py"
      to: "engine/src/jarvis_engine/ops_sync.py"
      via: "Narrative briefing consumes data from OpsSnapshot populated by ops_sync"
      pattern: "OpsSnapshot"
---

<objective>
Enhance the IMAP email reader with sender/date extraction and multi-signal importance triage, then build a two-stage daily briefing that assembles deterministic data and passes it through the Phase 3 ModelGateway for LLM narrative synthesis routed to local Ollama (privacy-safe, zero cost).

Purpose: The current email reader only extracts subjects with keyword-based importance. The daily briefing produces mechanical counts ("Urgent tasks: 3") rather than an actionable narrative. This plan upgrades both: emails get sender-aware triage, and the briefing becomes a coherent morning brief the owner can act on.

Output: Enhanced `ops_sync.py` email reader, `life_ops.py` with `build_narrative_brief()`, updated `OpsBriefHandler`, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-connectors-and-daily-intelligence/04-RESEARCH.md
@.planning/phases/04-connectors-and-daily-intelligence/04-01-SUMMARY.md
@.planning/phases/03-intelligence-routing/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance IMAP email reader with sender/date extraction and multi-signal triage</name>
  <files>
    engine/src/jarvis_engine/ops_sync.py
  </files>
  <action>
    1. Upgrade `load_email_items()` in `engine/src/jarvis_engine/ops_sync.py`:
       - Add `timeout=30` parameter to `IMAP4_SSL(host, timeout=30)` constructor call.
       - Change `client.select("INBOX")` to `client.select("INBOX", readonly=True)` for read-only safety.
       - Extract `From` and `Date` headers alongside `Subject`:
         ```python
         sender = _decode_email_header(msg.get("From", ""))
         date_str = msg.get("Date", "")
         ```
       - Update the items.append() dict to include new fields:
         ```python
         items.append({
             "subject": subject,
             "from": sender,
             "date": date_str,
             "read": False,
             "importance": importance,
         })
         ```
       - Change importance assignment to use `_triage_email(sender, subject)` instead of `_email_importance(subject)`.

    2. Replace `_email_importance(subject)` with `_triage_email(sender: str, subject: str) -> str`:
       - Multi-signal classification:
         - High priority markers in subject: "urgent", "action required", "payment due", "invoice", "security", "incident", "deadline", "expiring", "overdue".
         - High priority markers in sender: patterns suggesting automated alerts (e.g., "noreply@", "alert@", "billing@", "security@").
         - If any high marker matches, return "high".
         - Otherwise return "normal".
       - Keep the old `_email_importance()` function body as a private helper or remove it and update any references.

    3. Ensure the JSON feed path (`JARVIS_EMAIL_JSON`) still works: if env var is set, load from JSON without IMAP. The existing `from` and `date` fields are optional in the JSON path.
  </action>
  <verify>
    - `python -c "from jarvis_engine.ops_sync import load_email_items; print('OK')"` succeeds
    - `cd engine && python -m pytest tests/ -x -q` -- all existing tests pass
  </verify>
  <done>
    IMAP email reader extracts From, Date, and Subject headers. Connection uses timeout=30 and readonly=True. Multi-signal triage classifies importance using both sender and subject keywords. JSON feed path still works. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build two-stage narrative daily briefing and update OpsBriefHandler</name>
  <files>
    engine/src/jarvis_engine/life_ops.py
    engine/src/jarvis_engine/handlers/ops_handlers.py
    engine/tests/test_email_briefing.py
  </files>
  <action>
    1. Add `build_narrative_brief()` to `engine/src/jarvis_engine/life_ops.py`:
       - Import at top: `from __future__ import annotations`, `import os`.
       - New function `_assemble_data_summary(snapshot: OpsSnapshot) -> str`:
         - Condense snapshot data into a structured text summary under ~1500 tokens.
         - Include: today's date, calendar events (title + time for up to 10), top-10 tasks by priority (urgent/high first), top-10 unread emails by importance, due medications (name + dose + time), due bills (name + amount).
         - Format as labeled sections, not raw JSON.
         - Each section max 5-8 items with truncated details.
       - New function `build_narrative_brief(snapshot: OpsSnapshot, gateway=None, memory_context: str = "") -> str`:
         - Stage 1: Call `_assemble_data_summary(snapshot)` for condensed data.
         - Stage 2: If `gateway` is not None, build a prompt:
           ```
           You are Jarvis, a personal AI assistant. Generate a concise, actionable
           morning briefing for the owner based on this data. Prioritize by urgency.
           Be specific about times and actions needed. Keep it under 250 words.

           TODAY'S DATA:
           {data_summary}

           RELEVANT MEMORY CONTEXT:
           {memory_context or "No additional context."}

           Generate the morning briefing:
           ```
         - Call `gateway.complete(messages=[{"role": "user", "content": prompt}], model=local_model, max_tokens=512, route_reason="daily_briefing_narrative")` where `local_model = os.environ.get("JARVIS_LOCAL_MODEL", "qwen3:14b")`.
         - If `gateway.complete()` returns a response with non-empty `.text`, return it.
         - If gateway is None or LLM call fails (empty text, exception), fall back to `build_daily_brief(snapshot)`.
       - Keep the existing `build_daily_brief()` function completely unchanged (it is the fallback).

    2. Update `OpsBriefHandler` in `engine/src/jarvis_engine/handlers/ops_handlers.py`:
       - Add an optional `gateway` parameter to the constructor: `def __init__(self, root: Path, gateway=None)`.
       - Store as `self._gateway = gateway`.
       - In `handle()`, after loading snapshot:
         - Try `build_narrative_brief(snapshot, gateway=self._gateway)` first.
         - If narrative is non-empty, use it. Otherwise fall back to `build_daily_brief(snapshot)`.
       - Update the lazy import to include `build_narrative_brief`.
       - Note: The DI composition root (`create_app()`) will need to pass the gateway to OpsBriefHandler. Add `gateway=None` as a keyword argument so it remains backward compatible. If `create_app()` already wires a gateway, pass it through. If not, the handler works without it (fallback to deterministic brief).

    3. Create `engine/tests/test_email_briefing.py` with these tests:

       **Email Triage Tests:**
       - `test_triage_email_high_subject`: Call `_triage_email("alice@example.com", "URGENT: server down")`. Assert returns "high".
       - `test_triage_email_high_sender`: Call `_triage_email("billing@company.com", "Monthly invoice")`. Assert returns "high" (billing@ sender pattern).
       - `test_triage_email_normal`: Call `_triage_email("friend@gmail.com", "Weekend plans")`. Assert returns "normal".
       - `test_triage_email_payment_due`: Call `_triage_email("noreply@bank.com", "Payment due reminder")`. Assert returns "high".

       **Narrative Briefing Tests:**
       - `test_build_narrative_brief_no_gateway`: Create an OpsSnapshot. Call `build_narrative_brief(snapshot, gateway=None)`. Assert returns the deterministic brief (same as `build_daily_brief()`).
       - `test_build_narrative_brief_with_mock_gateway`: Create a mock gateway object with a `complete()` method that returns a mock GatewayResponse with `.text = "Good morning, sir..."`. Call `build_narrative_brief(snapshot, gateway=mock_gw)`. Assert returns the LLM text.
       - `test_build_narrative_brief_gateway_failure`: Create a mock gateway whose `complete()` raises an exception. Call `build_narrative_brief(snapshot, gateway=mock_gw)`. Assert falls back to deterministic brief (no crash).
       - `test_assemble_data_summary_truncation`: Create an OpsSnapshot with 50 calendar events. Call `_assemble_data_summary(snapshot)`. Assert the result contains at most 10 event entries (truncation working).

       **OpsBriefHandler Tests:**
       - `test_ops_brief_handler_without_gateway`: Create OpsBriefHandler(root, gateway=None). Verify it returns the deterministic brief.
       - `test_ops_brief_handler_with_gateway`: Create OpsBriefHandler(root, gateway=mock_gw). Verify it returns the narrative brief.

       **Existing Test Compatibility:**
       - `test_existing_build_daily_brief_unchanged`: Replicate the existing test_life_ops.py test data. Call `build_daily_brief()` directly. Assert same output as before (proves existing function is untouched).

    4. Run the full test suite.
  </action>
  <verify>
    - `cd engine && python -m pytest tests/test_email_briefing.py -v` -- all new tests pass
    - `cd engine && python -m pytest tests/test_life_ops.py -v` -- existing tests still pass
    - `cd engine && python -m pytest tests/ -x -q` -- zero regressions across all tests
  </verify>
  <done>
    Email triage uses multi-signal classification (sender + subject). Narrative briefing generates LLM-powered morning brief via ModelGateway routed to local Ollama. Falls back to deterministic brief when gateway unavailable. Data summary is condensed to prevent prompt bloat. OpsBriefHandler accepts optional gateway. 11+ new tests pass. All existing tests pass with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `cd engine && python -m pytest tests/test_email_briefing.py -v` -- all email/briefing tests pass
2. `cd engine && python -m pytest tests/test_life_ops.py -v` -- existing life_ops tests untouched
3. `cd engine && python -m pytest tests/ -x -q` -- zero regressions
4. `python -c "from jarvis_engine.life_ops import build_narrative_brief; print('importable')"` -- new function exists
5. `python -c "from jarvis_engine.ops_sync import _triage_email; print('importable')"` -- new triage function exists
</verification>

<success_criteria>
- IMAP reader extracts From, Date, Subject with timeout=30 and readonly=True
- _triage_email() classifies importance using sender + subject multi-signal approach
- build_narrative_brief() generates LLM narrative via ModelGateway routed to local Ollama
- Fallback to build_daily_brief() when gateway is None or LLM fails
- Data summary condensed to ~1500 tokens before LLM prompt
- OpsBriefHandler accepts optional gateway parameter (backward compatible)
- 11+ new tests cover email triage, narrative generation, fallback, truncation, handler integration
- All existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-connectors-and-daily-intelligence/04-02-SUMMARY.md`
</output>
