---
phase: 20-infrastructure-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/jarvis_engine/agent/__init__.py
  - engine/src/jarvis_engine/agent/vram_coordinator.py
  - engine/src/jarvis_engine/ops/unity_process_manager.py
  - engine/src/jarvis_engine/commands/agent_commands.py
  - engine/src/jarvis_engine/handlers/agent_handlers.py
  - engine/src/jarvis_engine/app.py
  - engine/tests/test_vram_coordinator.py
  - engine/tests/test_unity_process_manager.py
  - engine/tests/test_agent_commands.py
autonomous: true
requirements: [UNITY-06, AGENT-04]

must_haves:
  truths:
    - "VRAM coordinator mutex prevents concurrent Ollama inference and Unity play-mode GPU use"
    - "Unity process tree can be launched, tracked, and fully terminated without orphaned processes"
    - "Agent CQRS commands (run, status, approve) are registered on the bus and return stub results"
  artifacts:
    - path: "engine/src/jarvis_engine/agent/__init__.py"
      provides: "Agent subpackage marker"
    - path: "engine/src/jarvis_engine/agent/vram_coordinator.py"
      provides: "VRAMCoordinator mutex with acquire/release for generation and playmode"
      exports: ["VRAMCoordinator", "get_coordinator", "read_vram_used_mb"]
    - path: "engine/src/jarvis_engine/ops/unity_process_manager.py"
      provides: "Unity process tree kill and stale lockfile cleanup"
      exports: ["kill_unity_tree", "ensure_unity_not_running", "UNITY_SERVICE_NAME"]
    - path: "engine/src/jarvis_engine/commands/agent_commands.py"
      provides: "AgentRunCommand, AgentStatusCommand, AgentApproveCommand dataclasses"
      exports: ["AgentRunCommand", "AgentRunResult", "AgentStatusCommand", "AgentStatusResult", "AgentApproveCommand", "AgentApproveResult"]
    - path: "engine/src/jarvis_engine/handlers/agent_handlers.py"
      provides: "Stub handlers returning not-yet-implemented results"
      exports: ["AgentRunHandler", "AgentStatusHandler", "AgentApproveHandler"]
    - path: "engine/src/jarvis_engine/app.py"
      provides: "Agent commands registered on existing CQRS bus"
  key_links:
    - from: "engine/src/jarvis_engine/app.py"
      to: "engine/src/jarvis_engine/commands/agent_commands.py"
      via: "bus.register with lazy-import handler factory"
      pattern: "bus\\.register\\(Agent"
    - from: "engine/src/jarvis_engine/ops/unity_process_manager.py"
      to: "engine/src/jarvis_engine/ops/process_manager.py"
      via: "reuses write_pid_file/read_pid_file/remove_pid_file"
      pattern: "from jarvis_engine\\.ops\\.process_manager import"
---

<objective>
Build the VRAM coordinator, Unity process manager, and CQRS agent command stubs -- the three infrastructure pieces that prevent OOM crashes, orphaned processes, and provide the command surface for the agent subsystem.

Purpose: These are blocking prerequisites. VRAM OOM and orphaned Unity processes are immediate, silent failure modes that strike on first combined Ollama+Unity use. The CQRS stubs define the public API surface that Phase 22 fills in.
Output: agent/ subpackage created, VRAMCoordinator singleton, UnityProcessManager, 3 CQRS commands registered on the bus, all with tests.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-infrastructure-foundations/20-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From engine/src/jarvis_engine/commands/base.py:
```python
@dataclass
class ResultBase:
    return_code: int = 0
    message: str = ""
```

From engine/src/jarvis_engine/ops/process_manager.py:
```python
SERVICES = ("daemon", "mobile_api", "widget")
def write_pid_file(service: str, root: Path) -> None: ...
def read_pid_file(service: str, root: Path) -> dict[str, Any] | None: ...
def remove_pid_file(service: str, root: Path) -> None: ...
```

From engine/src/jarvis_engine/app.py (registration pattern):
```python
# Lazy-import handler factory pattern:
bus.register(SomeCommand, SomeHandler(root).handle)
# Handler classes have a .handle(cmd) -> Result method
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VRAMCoordinator and UnityProcessManager with tests</name>
  <files>
    engine/src/jarvis_engine/agent/__init__.py,
    engine/src/jarvis_engine/agent/vram_coordinator.py,
    engine/src/jarvis_engine/ops/unity_process_manager.py,
    engine/tests/test_vram_coordinator.py,
    engine/tests/test_unity_process_manager.py
  </files>
  <behavior>
    - VRAMCoordinator: acquire_generation blocks acquire_playmode (and vice versa) -- mutex is mutually exclusive
    - VRAMCoordinator: release allows the other side to acquire
    - VRAMCoordinator: status property returns dict with generation_active, playmode_active, locked
    - VRAMCoordinator: get_coordinator() returns singleton instance
    - read_vram_used_mb: returns int MB on success, None on failure (mock nvidia-smi)
    - read_vram_used_mb: returns None if nvidia-smi not found (FileNotFoundError)
    - kill_unity_tree: on Windows calls taskkill /f /t /pid; returns True on success
    - kill_unity_tree: returns False if process not found
    - ensure_unity_not_running: kills stale Unity if lockfile exists with live PID
    - ensure_unity_not_running: no-op if no lockfile exists
  </behavior>
  <action>
    Create `engine/src/jarvis_engine/agent/__init__.py` as empty file marking the new subpackage.

    Create `engine/src/jarvis_engine/agent/vram_coordinator.py` following the exact pattern from 20-RESEARCH.md Pattern 1:
    - VRAMCoordinator dataclass with asyncio.Lock as _gpu_mutex
    - async acquire_generation / release_generation
    - async acquire_playmode / release_playmode
    - status property returning dict
    - read_vram_used_mb() function using subprocess.run calling nvidia-smi
    - Module-level get_coordinator() singleton factory
    - VRAM_PRESSURE_THRESHOLD_MB = 7500

    Create `engine/src/jarvis_engine/ops/unity_process_manager.py` following 20-RESEARCH.md Pattern 2:
    - UNITY_SERVICE_NAME = "unity_editor"
    - kill_unity_tree(pid: int) -> bool: uses taskkill /f /t on Windows, os.killpg on POSIX
    - ensure_unity_not_running(root: Path) -> None: reads PID file via process_manager, kills tree if alive, removes PID file
    - Import from ops.process_manager: read_pid_file, remove_pid_file

    IMPORTANT: Use asyncio.Lock default_factory correctly (field(default_factory=asyncio.Lock, init=False)). Do NOT call subprocess.run for nvidia-smi on the async hot path -- read_vram_used_mb is a sync utility for checkpoint boundaries only.
  </action>
  <verify>
    <automated>cd C:/Users/Conner/jarvis-memory-repo && python -m pytest engine/tests/test_vram_coordinator.py engine/tests/test_unity_process_manager.py -x -q</automated>
  </verify>
  <done>VRAMCoordinator mutex tested (concurrent acquire blocked), nvidia-smi mock tested, Unity process tree kill tested with subprocess mock, ensure_unity_not_running tested with/without lockfile</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent CQRS commands, stub handlers, and bus registration</name>
  <files>
    engine/src/jarvis_engine/commands/agent_commands.py,
    engine/src/jarvis_engine/handlers/agent_handlers.py,
    engine/src/jarvis_engine/app.py,
    engine/tests/test_agent_commands.py
  </files>
  <behavior>
    - AgentRunCommand(goal="test") is a frozen dataclass with goal, task_id, token_budget fields
    - AgentStatusCommand(task_id="x") is a frozen dataclass
    - AgentApproveCommand(task_id="x", approved=True) is a frozen dataclass
    - AgentRunHandler.handle returns AgentRunResult with return_code=0, message containing "not yet implemented"
    - AgentStatusHandler.handle returns AgentStatusResult with return_code=0
    - AgentApproveHandler.handle returns AgentApproveResult with return_code=0
    - All three commands are registered on the CQRS bus (bus.execute returns a result)
  </behavior>
  <action>
    Create `engine/src/jarvis_engine/commands/agent_commands.py` following 20-RESEARCH.md Pattern 6:
    - AgentRunCommand (frozen dataclass): goal: str = "", task_id: str = "", token_budget: int = 50000
    - AgentRunResult (ResultBase): task_id: str = "", status: str = ""
    - AgentStatusCommand (frozen dataclass): task_id: str = ""
    - AgentStatusResult (ResultBase): task_id, status, step_index, tokens_used, last_error
    - AgentApproveCommand (frozen dataclass): task_id: str = "", approved: bool = True, reason: str = ""
    - AgentApproveResult (ResultBase): task_id: str = "", action_taken: str = ""

    Create `engine/src/jarvis_engine/handlers/agent_handlers.py`:
    - Three handler classes (AgentRunHandler, AgentStatusHandler, AgentApproveHandler)
    - Each takes root: Path in __init__, has handle(cmd) -> Result method
    - All return stub results with return_code=0, message="Agent subsystem not yet implemented"
    - Follow the exact pattern of existing handlers (e.g., ops_handlers.py)

    Modify `engine/src/jarvis_engine/app.py`:
    - Add import of agent command classes at top of the registration section
    - Add _register_agent_handlers(bus, root) function with lazy imports of agent_handlers
    - Call it from the main registration flow (find where other _register_* functions are called)

    IMPORTANT: Use lazy-import pattern inside _register_agent_handlers (import handlers inside the function, not at module level). Register on the EXISTING bus -- never create a second CommandBus.
  </action>
  <verify>
    <automated>cd C:/Users/Conner/jarvis-memory-repo && python -m pytest engine/tests/test_agent_commands.py -x -q</automated>
  </verify>
  <done>Three agent commands importable, three stub handlers return results, all three registered on CQRS bus (verified by bus.execute round-trip test), existing tests still pass</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/Conner/jarvis-memory-repo
python -m pytest engine/tests/test_vram_coordinator.py engine/tests/test_unity_process_manager.py engine/tests/test_agent_commands.py -x -q
ruff check engine/src/jarvis_engine/agent/ engine/src/jarvis_engine/ops/unity_process_manager.py engine/src/jarvis_engine/commands/agent_commands.py engine/src/jarvis_engine/handlers/agent_handlers.py
python -m pytest engine/tests/ -x -q  # Full suite -- no regressions
```
</verification>

<success_criteria>
- VRAMCoordinator singleton acquires/releases mutex correctly
- read_vram_used_mb returns int or None
- kill_unity_tree uses taskkill /f /t on Windows
- ensure_unity_not_running handles stale lockfile
- AgentRunCommand, AgentStatusCommand, AgentApproveCommand registered on bus
- Stub handlers return return_code=0
- All existing 5979+ tests still pass
- ruff clean on all new files
</success_criteria>

<output>
After completion, create `.planning/phases/20-infrastructure-foundations/20-01-SUMMARY.md`
</output>
