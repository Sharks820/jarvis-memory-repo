---
phase: 20-infrastructure-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/jarvis_engine/agent/state_store.py
  - engine/src/jarvis_engine/agent/tool_registry.py
  - engine/src/jarvis_engine/data/unity_kg_seed/unity63_api.json
  - engine/src/jarvis_engine/data/unity_kg_seed/unity63_breaking.json
  - engine/src/jarvis_engine/data/unity_kg_seed/unity63_errors.json
  - engine/src/jarvis_engine/agent/kg_seeder.py
  - engine/tests/test_agent_state_store.py
  - engine/tests/test_tool_registry.py
  - engine/tests/test_kg_seeder.py
autonomous: true
requirements: [KNOW-01, TOOL-01, AGENT-04]

must_haves:
  truths:
    - "Unity 6.3 API reference, breaking changes, and common error patterns are queryable in the knowledge graph"
    - "AgentStateStore persists task plans and checkpoints to SQLite -- task survives simulated crash and resumes from last checkpoint"
    - "ToolRegistry accepts tool registrations with JSON Schema descriptors and approval flags; registered tools are discoverable"
  artifacts:
    - path: "engine/src/jarvis_engine/agent/state_store.py"
      provides: "AgentTask dataclass and AgentStateStore with checkpoint/load/resume"
      exports: ["AgentTask", "AgentStateStore"]
    - path: "engine/src/jarvis_engine/agent/tool_registry.py"
      provides: "ToolSpec dataclass and ToolRegistry with register/get/list/schemas_for_prompt"
      exports: ["ToolSpec", "ToolCallable", "ToolRegistry"]
    - path: "engine/src/jarvis_engine/agent/kg_seeder.py"
      provides: "Idempotent Unity 6.3 KG seeding function"
      exports: ["seed_unity_kg", "is_unity_kg_seeded"]
    - path: "engine/src/jarvis_engine/data/unity_kg_seed/unity63_api.json"
      provides: "Unity 6.3 API type/method dictionary"
    - path: "engine/src/jarvis_engine/data/unity_kg_seed/unity63_breaking.json"
      provides: "Unity 6.3 breaking changes catalog"
    - path: "engine/src/jarvis_engine/data/unity_kg_seed/unity63_errors.json"
      provides: "Common Unity compile error to fix patterns"
  key_links:
    - from: "engine/src/jarvis_engine/agent/state_store.py"
      to: "sqlite3.Connection"
      via: "Accepts existing MemoryEngine DB connection (never opens its own)"
      pattern: "def __init__.*db.*Connection"
    - from: "engine/src/jarvis_engine/agent/kg_seeder.py"
      to: "engine/src/jarvis_engine/knowledge/graph.py"
      via: "Calls graph.add_fact() for each seed entry"
      pattern: "add_fact"
    - from: "engine/src/jarvis_engine/agent/tool_registry.py"
      to: "LLM system prompt"
      via: "schemas_for_prompt() returns list of tool schemas for prompt injection"
      pattern: "schemas_for_prompt"
---

<objective>
Build the AgentStateStore for crash-safe checkpointing, the ToolRegistry for pluggable tool discovery, and the Unity 6.3 KG seeder -- the three data/contract foundations that all later agent phases depend on.

Purpose: Without AgentStateStore, any crash loses agent task state. Without ToolRegistry, Phase 22 has no tool discovery contract. Without KG seeding, Phase 23 code generation hallucinates non-existent Unity 6.3 APIs.
Output: AgentStateStore with SQLite table, ToolRegistry with JSON Schema support, KG seeder with Unity 6.3 API/breaking/error data, all with tests.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-infrastructure-foundations/20-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From engine/src/jarvis_engine/knowledge/graph.py:
```python
class KnowledgeGraph:
    def add_fact(
        self,
        node_id: str,
        label: str,
        confidence: float,
        source_record: str = "",
        node_type: str = "fact",
    ) -> bool:
        """Add or update a fact node. Returns False if blocked by lock."""
```

From engine/src/jarvis_engine/commands/base.py:
```python
@dataclass
class ResultBase:
    return_code: int = 0
    message: str = ""
```

From engine/src/jarvis_engine/knowledge/_base.py:
```python
def upsert_fts_kg(db: sqlite3.Connection, node_id: str, label: str, tags: str) -> None:
    """Upsert into FTS5 virtual table for KG full-text search."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentStateStore and ToolRegistry with tests</name>
  <files>
    engine/src/jarvis_engine/agent/state_store.py,
    engine/src/jarvis_engine/agent/tool_registry.py,
    engine/tests/test_agent_state_store.py,
    engine/tests/test_tool_registry.py
  </files>
  <behavior>
    - AgentTask dataclass holds task_id, goal, status, plan_json, step_index, checkpoint_json, token_budget, tokens_used, error_count, last_error, approval_needed
    - AgentStateStore(db) accepts sqlite3.Connection and creates agent_tasks table via CREATE TABLE IF NOT EXISTS
    - checkpoint(task) inserts/replaces full task state in agent_tasks
    - load(task_id) returns AgentTask or None
    - load returns None for non-existent task_id
    - Crash recovery: checkpoint at step 3, reload, step_index == 3
    - list_by_status("running") returns only running tasks
    - ToolSpec dataclass with name, description, parameters (JSON Schema dict), execute, validate, estimate_cost, requires_approval, is_destructive
    - ToolSpec: is_destructive=True forces requires_approval=True via __post_init__
    - ToolRegistry.register(spec) stores tool by name
    - ToolRegistry.get("name") returns ToolSpec or None
    - ToolRegistry.list_tools() returns all registered specs
    - ToolRegistry.schemas_for_prompt() returns list of dicts with name, description, parameters, requires_approval
    - ToolRegistry: registering same name overwrites (with warning log)
  </behavior>
  <action>
    Create `engine/src/jarvis_engine/agent/state_store.py` following 20-RESEARCH.md Pattern 4:
    - AgentTask dataclass with all fields from the schema
    - AgentStateStore class accepting sqlite3.Connection in __init__
    - _ensure_schema creates agent_tasks table + status index via CREATE TABLE IF NOT EXISTS
    - checkpoint(task: AgentTask) -> None: INSERT OR REPLACE with all fields, updates updated_at
    - load(task_id: str) -> AgentTask | None: SELECT * WHERE task_id = ?, returns None if not found
    - list_by_status(status: str) -> list[AgentTask]: SELECT WHERE status = ?
    - delete(task_id: str) -> bool: DELETE WHERE task_id = ?, returns True if row deleted
    - IMPORTANT: Accept existing DB connection, NEVER open new sqlite3.connect(). Use self._db.commit() after writes.

    Create `engine/src/jarvis_engine/agent/tool_registry.py` following 20-RESEARCH.md Pattern 5:
    - ToolCallable Protocol: async def __call__(self, **kwargs: Any) -> Any
    - ToolSpec dataclass: name, description, parameters (dict), execute (Callable), validate (Callable with default), estimate_cost (Callable with default), requires_approval (bool), is_destructive (bool)
    - __post_init__ sets requires_approval=True if is_destructive=True
    - ToolRegistry class with _tools: dict[str, ToolSpec]
    - register(spec: ToolSpec) -> None: logs warning on overwrite
    - get(name: str) -> ToolSpec | None
    - list_tools() -> list[ToolSpec]
    - schemas_for_prompt() -> list[dict]: returns name/description/parameters/requires_approval for each tool
    - __len__() -> int

    Tests use in-memory SQLite (:memory:) for AgentStateStore. Tests use simple async mock callables for ToolSpec.execute.
  </action>
  <verify>
    <automated>cd C:/Users/Conner/jarvis-memory-repo && python -m pytest engine/tests/test_agent_state_store.py engine/tests/test_tool_registry.py -x -q</automated>
  </verify>
  <done>AgentStateStore checkpoint/load round-trip works, crash recovery (checkpoint at step N, reload shows step N), ToolRegistry register/get/list/schemas_for_prompt all tested, is_destructive forces approval</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unity 6.3 KG seed data and seeder script with tests</name>
  <files>
    engine/src/jarvis_engine/data/unity_kg_seed/unity63_api.json,
    engine/src/jarvis_engine/data/unity_kg_seed/unity63_breaking.json,
    engine/src/jarvis_engine/data/unity_kg_seed/unity63_errors.json,
    engine/src/jarvis_engine/agent/kg_seeder.py,
    engine/tests/test_kg_seeder.py
  </files>
  <behavior>
    - seed_unity_kg(kg) loads all three JSON files and calls kg.add_fact for each entry
    - is_unity_kg_seeded(kg) returns True after seeding, False before
    - seed_unity_kg is idempotent -- calling twice does not duplicate entries
    - unity63_api.json contains at least 30 entries covering core Unity types/methods (GameObject, Transform, MonoBehaviour, Component, etc.)
    - unity63_breaking.json contains at least 10 entries for Unity 6.3 breaking changes (removed namespaces, serialization changes, render pipeline)
    - unity63_errors.json contains at least 10 entries mapping CS error codes to fix patterns
    - All seed entries have node_id, label, confidence, source_record, node_type fields
  </behavior>
  <action>
    Create `engine/src/jarvis_engine/data/unity_kg_seed/` directory.

    Create `unity63_api.json`: JSON array of objects, each with:
    - node_id: "unity63_api_{ClassName}_{MethodName}" (unique identifier)
    - label: Full method/type signature (e.g., "GameObject.AddComponent<T>() - Returns T component")
    - confidence: 0.95
    - source_record: "unity63_kg_seed_v1"
    - node_type: "unity_api"
    - tags: ["unity63", "api_reference", "{Namespace}"]
    Include at least 30 entries covering: GameObject, Transform, MonoBehaviour lifecycle (Awake/Start/Update/FixedUpdate/OnDestroy), Component, Rigidbody, Collider, Camera, Light, Material, Renderer, Input (legacy + new), SceneManager, AssetDatabase, EditorWindow, SerializeField, RequireComponent, AddComponent, GetComponent, Instantiate, Destroy, FindObjectOfType, Resources.Load.

    Create `unity63_breaking.json`: JSON array covering at least 10 breaking changes:
    - UnityEngine.Experimental.* namespaces removed
    - [SerializeField] on properties requires [field: SerializeField] syntax
    - Compatibility mode render graph calls removed in 6.3
    - UnityEngine.Rendering.Universal compat APIs removed
    - Physics OnTriggerEnter/OnCollisionEnter signature changes
    - Input.GetAxis deprecated in favor of InputSystem
    - WWW class fully removed (use UnityWebRequest)
    - EditorGUILayout.* signature changes
    - Mesh.vertices direct assignment deprecated (use SetVertices)
    - AssetDatabase.Refresh force mode changes

    Create `unity63_errors.json`: JSON array mapping at least 10 common compile errors to fixes:
    - CS0117 (member not found) -> KG lookup for correct API name
    - CS0619 (deprecated) -> KG lookup for replacement
    - CS0246 (type not found) -> Check namespace removals
    - CS0103 (name not in scope) -> using directive missing
    - CS0029 (type conversion) -> Unity type cast patterns
    - CS1061 (member not defined) -> API renamed in 6.x
    - CS0428 (method group vs invocation) -> Unity callback patterns
    - CS0234 (namespace member missing) -> Experimental namespace removed
    - CS0120 (instance ref needed) -> static vs instance in Unity 6.3
    - CS0535 (interface not implemented) -> interface changes in 6.x

    Create `engine/src/jarvis_engine/agent/kg_seeder.py`:
    - seed_unity_kg(kg: KnowledgeGraph) -> int: Loads all three JSON files from data/unity_kg_seed/, calls kg.add_fact() for each entry, returns count of facts seeded
    - is_unity_kg_seeded(kg: KnowledgeGraph) -> bool: Checks if any node with source_record="unity63_kg_seed_v1" exists
    - Use importlib.resources or Path(__file__).parent.parent / "data" / "unity_kg_seed" to locate JSON files
    - Log count of seeded facts at INFO level
    - IMPORTANT: Use existing kg.add_fact() method -- do NOT write raw SQL to kg_nodes table

    Tests mock the KnowledgeGraph (mock add_fact, track calls). Verify correct number of calls, correct node_type values, idempotency check.
  </action>
  <verify>
    <automated>cd C:/Users/Conner/jarvis-memory-repo && python -m pytest engine/tests/test_kg_seeder.py -x -q</automated>
  </verify>
  <done>Three JSON seed files exist with documented minimum entry counts, seeder loads them and calls add_fact correctly, is_unity_kg_seeded returns True after seeding and False before, idempotent re-seeding works</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/Conner/jarvis-memory-repo
python -m pytest engine/tests/test_agent_state_store.py engine/tests/test_tool_registry.py engine/tests/test_kg_seeder.py -x -q
ruff check engine/src/jarvis_engine/agent/state_store.py engine/src/jarvis_engine/agent/tool_registry.py engine/src/jarvis_engine/agent/kg_seeder.py
python -m pytest engine/tests/ -x -q  # Full suite -- no regressions
```
</verification>

<success_criteria>
- AgentStateStore checkpoint/load round-trip works with in-memory SQLite
- Crash recovery proven: checkpoint at step 3, reload, step_index == 3
- ToolRegistry register/get/list/schemas_for_prompt all work
- is_destructive forces requires_approval=True
- Three Unity 6.3 seed JSON files exist with documented minimum entries
- KG seeder loads files, calls add_fact for each entry
- Seeding is idempotent
- All existing 5979+ tests still pass
- ruff clean on all new files
</success_criteria>

<output>
After completion, create `.planning/phases/20-infrastructure-foundations/20-02-SUMMARY.md`
</output>
