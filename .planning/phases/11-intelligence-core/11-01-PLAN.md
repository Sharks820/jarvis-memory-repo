---
phase: 11-intelligence-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/jarvis/assistant/feature/callscreen/CallScreeningService.kt
  - android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamDatabase.kt
  - android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamScorer.kt
  - android/app/src/main/java/com/jarvis/assistant/data/dao/SpamDao.kt
  - android/app/src/main/java/com/jarvis/assistant/data/entity/SpamEntity.kt
  - android/app/src/main/java/com/jarvis/assistant/data/JarvisDatabase.kt
  - android/app/src/main/java/com/jarvis/assistant/di/AppModule.kt
  - android/app/src/main/java/com/jarvis/assistant/api/JarvisApi.kt
  - android/app/src/main/java/com/jarvis/assistant/api/models/ApiModels.kt
  - android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsViewModel.kt
  - android/app/build.gradle.kts
autonomous: true
requirements: [CALL-01, CALL-02, CALL-03, CALL-04]

must_haves:
  truths:
    - "When an unknown number calls, CallScreeningService intercepts before the phone rings"
    - "The call is scored against the local spam database using frequency, duration, and contact patterns"
    - "User-configured threshold determines whether a call is blocked, silenced, sent to voicemail, or allowed"
    - "The spam database syncs from the desktop phone_guard module via the API"
    - "User can configure call screening threshold and actions in Settings"
  artifacts:
    - path: "android/app/src/main/java/com/jarvis/assistant/feature/callscreen/CallScreeningService.kt"
      provides: "Android CallScreeningService that intercepts incoming calls"
      contains: "class CallScreeningService : CallScreeningService()"
    - path: "android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamScorer.kt"
      provides: "Scoring engine matching desktop phone_guard logic"
      exports: ["SpamScorer", "ScoreResult"]
    - path: "android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamDatabase.kt"
      provides: "Spam DB sync manager that pulls candidates from desktop"
      exports: ["SpamDatabaseSync"]
    - path: "android/app/src/main/java/com/jarvis/assistant/data/dao/SpamDao.kt"
      provides: "Room DAO for spam candidate records"
      contains: "interface SpamDao"
    - path: "android/app/src/main/java/com/jarvis/assistant/data/entity/SpamEntity.kt"
      provides: "Room entity for spam number records"
      contains: "data class SpamEntity"
  key_links:
    - from: "CallScreeningService"
      to: "SpamScorer"
      via: "Hilt injection, scores incoming call number"
      pattern: "spamScorer\\.score"
    - from: "SpamScorer"
      to: "SpamDao"
      via: "Room query for matching number"
      pattern: "spamDao\\.findByNumber"
    - from: "SpamDatabaseSync"
      to: "JarvisApi /command endpoint"
      via: "Fetches spam report from desktop phone_guard"
      pattern: "api\\(\\)\\.sendCommand"
---

<objective>
Implement call screening with local spam database sync, spam scoring engine, and user-configurable threshold actions.

Purpose: Protect the user from spam calls by intercepting unknown numbers before the phone rings, scoring them against a locally synced spam database from the desktop phone_guard module, and applying the user's configured action (block/silence/voicemail/allow).

Output: CallScreeningService, SpamScorer, SpamDatabase sync, Room entities/DAOs, Settings UI for call screening configuration.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-and-daily-driver/10-03-SUMMARY.md

Key existing files to read:
@android/app/src/main/java/com/jarvis/assistant/data/JarvisDatabase.kt
@android/app/src/main/java/com/jarvis/assistant/di/AppModule.kt
@android/app/src/main/java/com/jarvis/assistant/api/JarvisApi.kt
@android/app/src/main/java/com/jarvis/assistant/api/models/ApiModels.kt
@android/app/src/main/java/com/jarvis/assistant/service/JarvisService.kt
@android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsViewModel.kt
@android/app/src/main/AndroidManifest.xml
@android/app/build.gradle.kts

Desktop reference (spam scoring logic to mirror):
@engine/src/jarvis_engine/phone_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Room entities, DAOs, spam scorer, and desktop sync</name>
  <files>
    android/app/src/main/java/com/jarvis/assistant/data/entity/SpamEntity.kt
    android/app/src/main/java/com/jarvis/assistant/data/dao/SpamDao.kt
    android/app/src/main/java/com/jarvis/assistant/data/JarvisDatabase.kt
    android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamScorer.kt
    android/app/src/main/java/com/jarvis/assistant/feature/callscreen/SpamDatabase.kt
    android/app/src/main/java/com/jarvis/assistant/di/AppModule.kt
    android/app/src/main/java/com/jarvis/assistant/api/JarvisApi.kt
    android/app/src/main/java/com/jarvis/assistant/api/models/ApiModels.kt
    android/app/build.gradle.kts
  </files>
  <action>
    **SpamEntity** (`data/entity/SpamEntity.kt`): Create Room entity `@Entity(tableName = "spam_numbers")` with fields: `number: String` (PrimaryKey), `score: Float`, `calls: Int`, `missedRatio: Float`, `avgDurationS: Float`, `reasons: String` (JSON array stored as string), `lastSynced: Long`, `userAction: String` (one of "block", "silence", "voicemail", "allow", "auto" -- default "auto" means system decides based on score).

    **SpamDao** (`data/dao/SpamDao.kt`): Room DAO interface with:
    - `@Query("SELECT * FROM spam_numbers WHERE number = :number") suspend fun findByNumber(number: String): SpamEntity?`
    - `@Query("SELECT * FROM spam_numbers ORDER BY score DESC") fun getAllFlow(): Flow<List<SpamEntity>>`
    - `@Upsert suspend fun upsertAll(entries: List<SpamEntity>)`
    - `@Query("DELETE FROM spam_numbers WHERE lastSynced < :cutoff") suspend fun deleteStale(cutoff: Long)`

    **JarvisDatabase** update: Bump version to 2 (add `SpamEntity::class` to entities list, keep `fallbackToDestructiveMigration()`). Add `abstract fun spamDao(): SpamDao`.

    **SpamScorer** (`feature/callscreen/SpamScorer.kt`): Hilt `@Singleton` class injecting `SpamDao`. Provides:
    - `data class ScoreResult(val score: Float, val reasons: List<String>, val recommendedAction: String)`
    - `suspend fun score(phoneNumber: String): ScoreResult` -- queries SpamDao by normalized number. If found, returns stored score + reasons. `recommendedAction` determined by comparing score against thresholds read from EncryptedSharedPreferences (defaults: block >= 0.80, silence >= 0.60, voicemail >= 0.40, allow below 0.40). If not found in DB, returns ScoreResult(0f, emptyList(), "allow").
    - `fun normalizeNumber(number: String): String` -- port the `_normalize_number()` logic from desktop phone_guard.py: strip non-digit except +, handle 00 prefix, 10-digit US -> +1prefix, 11-digit starting with 1 -> +prefix, 8+ digits -> +prefix.
    - Threshold keys in SharedPreferences: `call_screen_block_threshold` (default 0.80), `call_screen_silence_threshold` (default 0.60), `call_screen_voicemail_threshold` (default 0.40).

    **SpamDatabaseSync** (`feature/callscreen/SpamDatabase.kt`): Hilt `@Singleton` class injecting `JarvisApiClient`, `SpamDao`. Provides:
    - `suspend fun syncFromDesktop()` -- sends command to desktop: `api().sendCommand(CommandRequest(text = "Jarvis, run spam scan", execute = true))`. Then sends a second request: `api().sendCommand(CommandRequest(text = "Jarvis, show spam report"))` to get the current spam candidates. Parses the stdout_tail response lines looking for JSON spam report data. For each candidate found, upserts into SpamDao with current timestamp as lastSynced. Deletes entries not synced in last 7 days via `deleteStale()`.
    - NOTE: The desktop phone_guard.py `detect_spam_candidates()` is the scoring engine. The sync pulls the desktop's pre-computed candidates. If parsing fails, log warning and keep existing local data (graceful degradation).

    **AppModule** update: Add `@Provides fun provideSpamDao(db: JarvisDatabase): SpamDao = db.spamDao()`. Add `@Provides @Singleton fun provideSpamScorer(spamDao: SpamDao, @ApplicationContext context: Context): SpamScorer`.

    **JarvisApi** update: Add new endpoint for spam data pull. Add:
    - `@GET("/spam/candidates") suspend fun getSpamCandidates(): SpamCandidatesResponse`
    NOTE: The desktop API does not have this endpoint yet. SpamDatabaseSync should fall back to using the /command endpoint with "show spam report" text if /spam/candidates returns 404. For initial implementation, use the /command endpoint approach exclusively since that's guaranteed to work with the existing desktop.

    **ApiModels** update: Add `data class SpamCandidatesResponse(val ok: Boolean = false, val candidates: List<SpamCandidateDto> = emptyList())` and `data class SpamCandidateDto(val number: String = "", val score: Float = 0f, val calls: Int = 0, @SerializedName("missed_ratio") val missedRatio: Float = 0f, @SerializedName("avg_duration_s") val avgDurationS: Float = 0f, val reasons: List<String> = emptyList())`.

    **build.gradle.kts**: No new dependencies needed (Room, Hilt, Retrofit already present).
  </action>
  <verify>
    Project compiles: `cd android && ./gradlew assembleDebug 2>&1 | tail -5` shows BUILD SUCCESSFUL.
    Verify SpamEntity is in the database entities list, SpamDao has all 4 query methods, SpamScorer has score() and normalizeNumber(), SpamDatabaseSync has syncFromDesktop().
  </verify>
  <done>
    SpamEntity persisted in Room with proper columns. SpamDao provides CRUD. SpamScorer normalizes numbers and returns score/action based on configurable thresholds. SpamDatabaseSync pulls candidates from desktop via /command endpoint. All injected via Hilt.
  </done>
</task>

<task type="auto">
  <name>Task 2: CallScreeningService, manifest registration, and Settings UI</name>
  <files>
    android/app/src/main/java/com/jarvis/assistant/feature/callscreen/CallScreeningService.kt
    android/app/src/main/AndroidManifest.xml
    android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsScreen.kt
    android/app/src/main/java/com/jarvis/assistant/ui/settings/SettingsViewModel.kt
    android/app/src/main/java/com/jarvis/assistant/service/JarvisService.kt
  </files>
  <action>
    **CallScreeningService** (`feature/callscreen/CallScreeningService.kt`): Extend `android.telecom.CallScreeningService`. This is a system-level service that Android routes incoming calls through when the app has the ROLE_CALL_SCREENING permission.

    Implementation:
    ```
    @AndroidEntryPoint
    class JarvisCallScreeningService : CallScreeningService() {
        @Inject lateinit var spamScorer: SpamScorer

        override fun onScreenCall(callDetails: Call.Details) {
            val number = callDetails.handle?.schemeSpecificPart ?: ""
            if (number.isBlank()) {
                respondToCall(callDetails, CallResponse.Builder().build())
                return
            }
            // Run scoring on IO dispatcher since it hits Room DB
            CoroutineScope(Dispatchers.IO + SupervisorJob()).launch {
                val result = spamScorer.score(spamScorer.normalizeNumber(number))
                val response = when (result.recommendedAction) {
                    "block" -> CallResponse.Builder()
                        .setDisallowCall(true)
                        .setRejectCall(true)
                        .setSkipCallLog(false)
                        .setSkipNotification(true)
                        .build()
                    "silence" -> CallResponse.Builder()
                        .setDisallowCall(false)
                        .setSilenceCall(true)
                        .setSkipCallLog(false)
                        .setSkipNotification(false)
                        .build()
                    "voicemail" -> CallResponse.Builder()
                        .setDisallowCall(true)
                        .setRejectCall(false)  // sends to voicemail
                        .setSkipCallLog(false)
                        .setSkipNotification(false)
                        .build()
                    else -> CallResponse.Builder().build()  // "allow" -- no action
                }
                respondToCall(callDetails, response)
            }
        }
    }
    ```

    IMPORTANT: Use `android.telecom.CallScreeningService`, NOT a custom base class. The service must be registered with the correct intent filter for `android.telecom.CallScreeningService`. The user must grant the call screening role via `RoleManager.ROLE_CALL_SCREENING` -- add a role request helper.

    Also add a utility function in the same file:
    ```
    fun requestCallScreeningRole(activity: Activity): ActivityResultLauncher<Intent>? // helper for onboarding
    ```
    And a composable-friendly check:
    ```
    fun isCallScreeningRoleGranted(context: Context): Boolean {
        val roleManager = context.getSystemService(RoleManager::class.java)
        return roleManager.isRoleHeld(RoleManager.ROLE_CALL_SCREENING)
    }
    ```

    **AndroidManifest.xml** updates:
    - Add permission: `<uses-permission android:name="android.permission.READ_PHONE_STATE" />`
    - Add permission: `<uses-permission android:name="android.permission.READ_CALL_LOG" />`
    - Register the service inside `<application>`:
    ```xml
    <service
        android:name=".feature.callscreen.JarvisCallScreeningService"
        android:permission="android.permission.BIND_SCREENING_SERVICE"
        android:exported="true">
        <intent-filter>
            <action android:name="android.telecom.CallScreeningService" />
        </intent-filter>
    </service>
    ```

    **JarvisService** update: Add spam database sync to the sync loop. After `processor.flushPending()`, add:
    ```kotlin
    try {
        spamDatabaseSync.syncFromDesktop()
    } catch (e: Exception) {
        Log.w(TAG, "Spam DB sync error: ${e.message}")
    }
    ```
    Inject `SpamDatabaseSync` via `@Inject lateinit var spamDatabaseSync: SpamDatabaseSync`. Only run spam sync every 10 minutes (not every 30s sync cycle) -- track `lastSpamSyncMs` and compare against `System.currentTimeMillis()`.

    **SettingsScreen.kt** update: Add a "Call Screening" section after the existing sections with:
    - Toggle switch: "Enable Call Screening" (persisted in SharedPreferences key `call_screen_enabled`, default true)
    - Slider: "Block Threshold" (0.0 to 1.0, default 0.80) -- stored as `call_screen_block_threshold`
    - Slider: "Silence Threshold" (0.0 to 1.0, default 0.60) -- stored as `call_screen_silence_threshold`
    - Slider: "Voicemail Threshold" (0.0 to 1.0, default 0.40) -- stored as `call_screen_voicemail_threshold`
    - Text showing current spam DB count: "Spam numbers tracked: {count}"
    - Button: "Request Call Screening Permission" that launches RoleManager request (only shown if role not held)
    - Use Compose Material 3 components (Slider, Switch, Text, Button) consistent with existing SettingsScreen style.

    **SettingsViewModel.kt** update: Add StateFlow properties for call screening settings. Add functions to update thresholds. Observe SpamDao count via `spamDao.getAllFlow().map { it.size }`. Inject SpamDao.
  </action>
  <verify>
    Project compiles: `cd android && ./gradlew assembleDebug 2>&1 | tail -5` shows BUILD SUCCESSFUL.
    AndroidManifest.xml contains the CallScreeningService registration with correct intent-filter.
    SettingsScreen contains call screening section with toggle and sliders.
    JarvisService sync loop includes spam DB sync with 10-minute throttle.
  </verify>
  <done>
    JarvisCallScreeningService registered in manifest, intercepts incoming calls, scores via SpamScorer, applies block/silence/voicemail/allow response. Spam DB syncs from desktop every 10 minutes in foreground service. Settings UI lets user configure thresholds and request call screening role. Unknown numbers scored against local database before phone rings.
  </done>
</task>

</tasks>

<verification>
1. `cd android && ./gradlew assembleDebug` compiles successfully
2. AndroidManifest.xml declares CallScreeningService with BIND_SCREENING_SERVICE permission and correct intent-filter
3. SpamEntity exists in Room database with all required columns
4. SpamScorer.normalizeNumber() handles: +1XXXXXXXXXX, 10-digit US, 11-digit with leading 1, international +XX formats
5. SpamScorer.score() returns correct action for scores at each threshold boundary
6. SpamDatabaseSync.syncFromDesktop() parses desktop command response into SpamEntity records
7. JarvisService sync loop includes spam sync with 10-minute interval throttle
8. SettingsScreen shows call screening section with threshold sliders and permission button
</verification>

<success_criteria>
- CallScreeningService intercepts calls and applies score-based actions before the phone rings
- Local spam database is populated from desktop phone_guard data via sync
- Spam scoring uses configurable thresholds (block >= 0.80, silence >= 0.60, voicemail >= 0.40)
- User can adjust all thresholds in Settings and request call screening role
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-intelligence-core/11-01-SUMMARY.md`
</output>
