---
phase: 01-voice-to-text-overhaul
plan: 05
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - engine/src/jarvis_engine/stt.py
  - engine/tests/test_stt.py
  - engine/tests/test_stt_vad.py
  - engine/tests/test_wakeword.py
autonomous: false
requirements: [STT-01, STT-02, STT-03, STT-04, STT-05, STT-06, STT-07, STT-08]

must_haves:
  truths:
    - "All 8 STT requirements have passing tests"
    - "Full fallback chain works end-to-end with mocked backends"
    - "Confidence scoring drives fallback decisions correctly"
    - "Personal vocabulary flows to both Deepgram keyterms and post-processing NER"
    - "All callers (voice_handlers, proactive_handlers, desktop_widget) work with new pipeline"
    - "No regressions in existing 4138+ test suite"
  artifacts:
    - path: "engine/tests/test_stt.py"
      provides: "Comprehensive integration tests for full STT pipeline"
      contains: "test_full_pipeline"
    - path: "engine/tests/test_stt_vad.py"
      provides: "VAD integration tests"
      min_lines: 60
  key_links:
    - from: "engine/src/jarvis_engine/handlers/voice_handlers.py"
      to: "engine/src/jarvis_engine/stt.py"
      via: "listen_and_transcribe() call"
      pattern: "listen_and_transcribe"
    - from: "engine/src/jarvis_engine/handlers/proactive_handlers.py"
      to: "engine/src/jarvis_engine/stt.py"
      via: "record_from_microphone() + transcribe_smart() calls"
      pattern: "transcribe_smart"

user_setup: []
---

<objective>
End-to-end integration testing and verification that all 8 STT requirements are satisfied, all callers work with the new pipeline, and no regressions exist.

Purpose: Verify the complete voice-to-text overhaul works as a system. Individual backends were tested in isolation (Plans 01-04); this plan tests the full pipeline: VAD -> recording -> transcription -> fallback -> post-processing -> callers.

Output: Integration test suite, verification of all 8 requirements, confirmation of zero regressions.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-voice-to-text-overhaul/01-RESEARCH.md
@.planning/phases/01-voice-to-text-overhaul/01-01-SUMMARY.md
@.planning/phases/01-voice-to-text-overhaul/01-02-SUMMARY.md
@.planning/phases/01-voice-to-text-overhaul/01-03-SUMMARY.md
@.planning/phases/01-voice-to-text-overhaul/01-04-SUMMARY.md
@engine/src/jarvis_engine/stt.py
@engine/src/jarvis_engine/stt_vad.py
@engine/src/jarvis_engine/wakeword.py
@engine/src/jarvis_engine/stt_postprocess.py
@engine/src/jarvis_engine/handlers/voice_handlers.py
@engine/src/jarvis_engine/handlers/proactive_handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for full STT pipeline</name>
  <files>engine/tests/test_stt.py</files>
  <action>
Add integration tests that verify the full pipeline end-to-end:

1. **test_full_pipeline_parakeet_happy_path**: Mock all backends. Parakeet returns high-confidence result. Verify:
   - transcribe_smart() returns Parakeet result
   - Post-processing applied (mock postprocess_transcription, verify called)
   - Metric logged (mock _log_stt_metric, verify called with backend="parakeet-tdt")
   - Deepgram/Groq/local backends never called

2. **test_full_pipeline_fallback_to_deepgram**: Parakeet fails (returns None), Deepgram succeeds. Verify:
   - Parakeet tried first (mock called)
   - Deepgram tried second with keyterms
   - Result backend is "deepgram-nova3"

3. **test_full_pipeline_fallback_to_groq**: Parakeet fails, Deepgram fails (no API key), Groq succeeds. Verify correct fallback order.

4. **test_full_pipeline_emergency_local**: All cloud/parakeet fail, faster-whisper large-v3 succeeds. Verify result backend is "faster-whisper" and emergency model used.

5. **test_full_pipeline_confidence_fallthrough**: Parakeet returns confidence=0.4 (below threshold), Deepgram returns confidence=0.9. Verify Deepgram result used (higher confidence above threshold).

6. **test_full_pipeline_postprocessing_integration**: Verify the post-processing pipeline (hallucination detection, filler removal, LLM correction, NER) still works with all backends. Use a realistic mock: backend returns "um uh Hello Connor", post-processing returns "Hello Conner" (filler removed + entity corrected).

7. **test_full_pipeline_personal_vocab_flows**: Verify personal_vocab.txt entries flow to:
   - Deepgram as keyterms (mock _try_deepgram, check keyterms param)
   - Post-processing as entity_list (mock postprocess_transcription, check entity_list param)

8. **test_listen_and_transcribe_uses_new_pipeline**: Mock record_from_microphone + transcribe_smart. Verify listen_and_transcribe() calls both and returns result from new fallback chain.

9. **test_caller_voice_handler_integration**: Verify VoiceListenHandler can call listen_and_transcribe() without errors (mock the STT functions, verify handler doesn't crash with new return format).

10. **test_caller_proactive_handler_integration**: Verify WakeWordStartHandler._on_detected() callback works with new record_from_microphone (Silero VAD) and transcribe_smart (fallback chain). Mock both functions.

Use `@patch.dict("os.environ", {"GROQ_API_KEY": "", "DEEPGRAM_API_KEY": ""})` as base, then selectively enable keys per test.
  </action>
  <verify>
`python -m pytest engine/tests/test_stt.py -x -q -k "full_pipeline or caller"` passes all integration tests.
  </verify>
  <done>10 integration tests cover the full pipeline: all fallback paths, post-processing, vocab flow, and caller compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix any regressions</name>
  <files>engine/tests/test_stt.py, engine/tests/test_stt_vad.py, engine/tests/test_wakeword.py</files>
  <action>
Run the complete test suite and fix any regressions:

1. Run `python -m pytest engine/tests/ -x -q --timeout=120` and capture output.

2. If any tests fail:
   - Categorize: is this a test that needs updating (references old API like `_confidence_retry`) or a real regression?
   - For tests referencing old API: update to use new API (FALLBACK_CHAIN, new forced modes)
   - For real regressions: fix the source code

3. Verify specific test files:
   - `python -m pytest engine/tests/test_stt.py -x -q` (all STT tests including new ones)
   - `python -m pytest engine/tests/test_stt_vad.py -x -q` (all VAD tests)
   - `python -m pytest engine/tests/test_wakeword.py -x -q` (all wake word tests)
   - `python -m pytest engine/tests/test_stt_postprocess.py -x -q` (post-processing unchanged)

4. Check that old tests referencing `_confidence_retry` are updated or removed (function was removed in Plan 04).

5. Check that tests referencing `JARVIS_STT_BACKEND=auto` still work with new fallback chain.

6. Verify total test count: should be 4138+ (original) + ~30 new tests = 4168+.

7. Create a requirements coverage summary verifying each STT requirement:
   - STT-01: _try_parakeet tests pass
   - STT-02: _try_deepgram tests pass (with keyterms)
   - STT-03: SileroVADDetector tests + record_from_microphone VAD tests pass
   - STT-04: record_from_microphone with Silero VAD streaming chunks passes
   - STT-05: wakeword.py Silero VAD integration tests pass
   - STT-06: Confidence scoring tests (all backends) pass
   - STT-07: Personal vocab + keyterm + NER tests pass
   - STT-08: FALLBACK_CHAIN integration tests pass (4-tier)
  </action>
  <verify>
`python -m pytest engine/tests/ -x -q --timeout=120` -- ALL tests pass, 0 failures.
Test count >= 4168 (4138 original + ~30 new).
  </verify>
  <done>Full test suite passes with 0 failures. All 8 STT requirements have corresponding passing tests. No regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete voice-to-text overhaul:
- New Silero VAD module (stt_vad.py) replacing energy-based VAD
- NVIDIA Parakeet TDT 0.6B backend via onnx-asr
- Deepgram Nova-3 backend with keyterm prompting from personal_vocab.txt
- 4-tier fallback chain: Parakeet -> Deepgram -> Groq -> faster-whisper large-v3
- Wake word detection upgraded with Silero VAD
- ~30 new tests, all passing
  </what-built>
  <how-to-verify>
1. Verify test suite passes: `python -m pytest engine/tests/ -x -q --timeout=120`
2. Verify new module imports:
   ```bash
   python -c "from jarvis_engine.stt_vad import SileroVADDetector; print('VAD module OK')"
   python -c "from jarvis_engine.stt import _try_parakeet, _try_deepgram, FALLBACK_CHAIN; print('Backends OK, chain:', [name for name, _ in FALLBACK_CHAIN])"
   ```
3. Check that no STT requirement is unaddressed by reviewing the SUMMARY
4. (Optional) If you have `pip install onnx-asr[cpu,hub] deepgram-sdk silero-vad`, test live:
   ```bash
   python -c "
   from jarvis_engine.stt import transcribe_smart
   import numpy as np
   # 2 seconds of silence - should return empty/low-confidence
   audio = np.zeros(32000, dtype=np.float32)
   result = transcribe_smart(audio)
   print(f'Backend: {result.backend}, Text: {result.text!r}, Confidence: {result.confidence}')
   "
   ```
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest engine/tests/ -x -q --timeout=120` -- full suite passes
2. All 8 STT requirements have corresponding tests
3. No regressions from original 4138 tests
4. New modules importable: stt_vad.py
5. Existing callers (voice_handlers, proactive_handlers, desktop_widget) verified
</verification>

<success_criteria>
- Full test suite passes: 4168+ tests, 0 failures
- Each of STT-01 through STT-08 has at least one dedicated passing test
- Integration tests verify full pipeline: VAD -> recording -> transcription -> fallback -> post-processing
- All existing callers work without modification
- Human verification confirms the overhaul is complete and correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-to-text-overhaul/01-05-SUMMARY.md`
</output>
