---
phase: 01-voice-to-text-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/jarvis_engine/stt.py
  - engine/tests/test_stt.py
autonomous: true
requirements: [STT-01, STT-06]

must_haves:
  truths:
    - "Parakeet TDT 0.6B transcribes audio via onnx-asr with <8% WER"
    - "_try_parakeet() returns TranscriptionResult with text and confidence"
    - "Parakeet model is lazy-loaded on first use (no startup cost)"
    - "Parakeet gracefully returns None if onnx-asr not installed"
    - "Confidence scoring uses log probabilities when available from onnx-asr"
  artifacts:
    - path: "engine/src/jarvis_engine/stt.py"
      provides: "_try_parakeet() backend function and _parakeet_model global"
      contains: "def _try_parakeet"
    - path: "engine/tests/test_stt.py"
      provides: "Tests for _try_parakeet backend"
      contains: "test_try_parakeet"
  key_links:
    - from: "engine/src/jarvis_engine/stt.py"
      to: "onnx_asr"
      via: "lazy import in _try_parakeet"
      pattern: "import onnx_asr"

user_setup: []
---

<objective>
Add the NVIDIA Parakeet TDT 0.6B backend to stt.py as a new `_try_parakeet()` function using the lightweight `onnx-asr` package.

Purpose: Parakeet TDT 0.6B has 6.05% WER vs the current faster-whisper small.en at ~15-20% WER -- a 2-3x accuracy improvement. Using onnx-asr (not nemo_toolkit) keeps the dependency footprint small (NumPy + ONNX Runtime only, no PyTorch for ASR).

Output: `_try_parakeet()` function in stt.py that follows the existing backend pattern, with proper confidence scoring and lazy model loading.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-voice-to-text-overhaul/01-RESEARCH.md
@engine/src/jarvis_engine/stt.py
@engine/tests/test_stt.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _try_parakeet() backend function</name>
  <files>engine/src/jarvis_engine/stt.py</files>
  <action>
Add the Parakeet backend to `stt.py`, placed after the existing `_try_local` function (before `_confidence_retry`):

1. **Module-level globals** (near `_local_stt_instance`):
   ```python
   _parakeet_model = None
   _parakeet_lock = threading.Lock()
   ```

2. **`_try_parakeet()` function** following the exact pattern of `_try_groq()` and `_try_local()`:
   ```python
   def _try_parakeet(
       audio: np.ndarray | str, *, language: str, prompt: str = ""
   ) -> TranscriptionResult | None:
   ```

   Implementation details:
   - Lazy import: `import onnx_asr` inside try/except ImportError -> return None with warning log
   - Lazy model load with double-checked locking (same pattern as `_local_stt_instance`):
     `_parakeet_model = onnx_asr.load_model("nemo-parakeet-tdt-0.6b-v2")`
   - Try loading with timestamps first: `model.with_timestamps()` to get log probabilities
     If `.with_timestamps()` raises or is not available, use base model
   - Transcription: `result = _parakeet_model.recognize(audio, sample_rate=16000)` for numpy arrays,
     `result = _parakeet_model.recognize(audio)` for file paths
   - Extract text: `text = str(result).strip()` if result else ""
   - **Confidence scoring** (STT-06):
     - If timestamps model is loaded and result has log probabilities, compute confidence
       using the same formula as Groq/faster-whisper: `min(1.0, max(0.0, 1.0 + avg_logprob))`
     - If log probs not available, use baseline confidence 0.94 (Parakeet's known 6.05% WER)
     - This is explicitly noted as an open question in research -- try `result.tokens` or similar
       attributes first; fall back to baseline if not available
   - Return `TranscriptionResult(text=text, language="en", confidence=confidence, duration_seconds=elapsed, backend="parakeet-tdt")`
   - Wrap the entire body in try/except Exception -> log warning, return None (never crash the caller)
   - Time the operation with `time.monotonic()` for duration_seconds

3. **Do NOT modify `transcribe_smart()` yet** -- the fallback chain rewrite happens in Plan 04. This plan only adds the backend function.

4. **Do NOT remove or modify** the existing `SpeechToText` class or `_try_local()` function. faster-whisper stays as the emergency fallback.
  </action>
  <verify>
`python -c "from jarvis_engine.stt import _try_parakeet; print('function exists')"` succeeds.
  </verify>
  <done>_try_parakeet() function exists in stt.py, follows existing backend pattern, returns TranscriptionResult or None.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for Parakeet backend</name>
  <files>engine/tests/test_stt.py</files>
  <action>
Add new test section to `engine/tests/test_stt.py` for the Parakeet backend:

1. **test_try_parakeet_success**: Mock `onnx_asr.load_model` to return a mock model. Mock model's `recognize()` returns "Hello world". Verify TranscriptionResult has text="Hello world", backend="parakeet-tdt", confidence > 0.

2. **test_try_parakeet_import_error**: Patch `builtins.__import__` to raise ImportError for onnx_asr. Verify _try_parakeet returns None (graceful degradation).

3. **test_try_parakeet_model_error**: Mock `onnx_asr.load_model` to raise RuntimeError. Verify _try_parakeet returns None (doesn't crash).

4. **test_try_parakeet_empty_result**: Mock model returning empty string. Verify result has text="" or returns None.

5. **test_try_parakeet_with_numpy_array**: Pass np.ndarray audio, verify `recognize(audio, sample_rate=16000)` called with correct args.

6. **test_try_parakeet_with_file_path**: Pass string file path, verify `recognize(path)` called without sample_rate.

7. **test_try_parakeet_lazy_model_load**: Call _try_parakeet twice, verify `load_model` called only once (singleton pattern).

8. **test_try_parakeet_confidence_baseline**: When no log probs available, verify confidence is 0.94 (Parakeet baseline).

All tests must mock onnx_asr completely -- no actual model download. Reset `_parakeet_model` global to None before each test that tests lazy loading (use monkeypatch or setUp). Use `@patch.dict("os.environ", ...)` where needed to isolate from real env.
  </action>
  <verify>
`python -m pytest engine/tests/test_stt.py -x -q -k "parakeet"` passes all parakeet tests.
`python -m pytest engine/tests/test_stt.py -x -q` passes all tests (existing + new).
  </verify>
  <done>8 new Parakeet backend tests pass. All existing STT tests still pass. No real model download required.</done>
</task>

</tasks>

<verification>
1. `python -m pytest engine/tests/test_stt.py -x -q` -- all tests pass (existing + new Parakeet tests)
2. `python -c "from jarvis_engine.stt import _try_parakeet"` -- import succeeds
3. No existing tests broken: `python -m pytest engine/tests/ -x -q --timeout=120`
</verification>

<success_criteria>
- _try_parakeet() function exists following the same pattern as _try_groq() and _try_local()
- Lazy model loading with thread-safe singleton pattern
- Confidence scoring uses log probs when available, falls back to 0.94 baseline
- Graceful degradation when onnx-asr not installed
- 8+ new tests all pass, existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-to-text-overhaul/01-02-SUMMARY.md`
</output>
