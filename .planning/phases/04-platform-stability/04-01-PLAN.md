# Plan 04-01: Platform Stability — All 5 STAB Requirements

**Phase:** 4 (Platform Stability)
**Requirements:** STAB-01 through STAB-05

## Tasks

### Task 1: STAB-01 — Remove db_path.exists() Gate
Remove the 3 `db_path.exists()` checks in app.py (lines 217, 256, 533) that gate brain
initialization. MemoryEngine._init_schema() uses CREATE IF NOT EXISTS, so the DB file
will be created on first run. Keep the try/except for graceful degradation.

### Task 2: STAB-02 — Add Logging to Silent Except Blocks
Add logger.debug() calls to 7 silent except blocks:
- learning_missions.py: 3 blocks in auto_generate_missions (Sources 1, 2, 3)
- learning/engine.py: 2 ImportError blocks (correction_detector, activity_feed)
- learning/metrics.py: 1 block (temporal distribution query)
- Narrow catch type in metrics.py from Exception to sqlite3.OperationalError

### Task 3: STAB-03 — ConsolidateMemoryCommand CQRS
Add ConsolidateMemoryCommand/Result to learning_commands.py, ConsolidateMemoryHandler
to learning_handlers.py, register in app.py, add CLI subcommand + daemon loop refactor.

### Task 4: STAB-04 — Proactive Trigger Diagnostics
Add diagnostics field to ProactiveCheckResult. When data sources are empty, include
diagnostic messages explaining which sources have no data and which connectors are not ready.

### Task 5: STAB-05 — Tests
Add tests for all 4 changes. Target: maintain 4200+ test count.

## Verification
- All tests pass (4200+)
- Fresh DB path creates engine, DB file, and all tables
- Silent except blocks now log at debug level
- ConsolidateMemoryCommand dispatches via bus, CLI, and daemon
- Proactive diagnostics show which data sources are empty
