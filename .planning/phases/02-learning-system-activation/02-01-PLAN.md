---
phase: 02-learning-system-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/jarvis_engine/commands/learning_commands.py
  - engine/src/jarvis_engine/learning/engine.py
  - engine/src/jarvis_engine/handlers/learning_handlers.py
  - engine/src/jarvis_engine/main.py
  - engine/src/jarvis_engine/app.py
  - engine/tests/test_learning_route_params.py
autonomous: true
requirements: [LEARN-04]

must_haves:
  truths:
    - "LearnInteractionCommand accepts route and topic fields"
    - "record_feedback() receives the actual classified route at all dispatch sites"
    - "record_interaction() receives the actual route and topic at all dispatch sites"
    - "Trackers are accessible from the bus via bus._pref_tracker, bus._feedback_tracker, bus._usage_tracker"
    - "Existing tests still pass (backward compatible defaults)"
  artifacts:
    - path: "engine/src/jarvis_engine/commands/learning_commands.py"
      provides: "LearnInteractionCommand with route and topic fields"
      contains: "route: str"
    - path: "engine/src/jarvis_engine/learning/engine.py"
      provides: "learn_from_interaction accepting and forwarding route/topic"
      contains: "route=route"
    - path: "engine/src/jarvis_engine/handlers/learning_handlers.py"
      provides: "Handler passing route/topic from command to engine"
      contains: "cmd.route"
    - path: "engine/src/jarvis_engine/app.py"
      provides: "Bus exposure of all 3 trackers"
      contains: "bus._pref_tracker"
    - path: "engine/tests/test_learning_route_params.py"
      provides: "Tests for route/topic propagation and bus exposure"
      min_lines: 60
  key_links:
    - from: "engine/src/jarvis_engine/main.py"
      to: "LearnInteractionCommand"
      via: "route kwarg at 4 dispatch sites"
      pattern: "route=_route|route=intent"
    - from: "engine/src/jarvis_engine/handlers/learning_handlers.py"
      to: "engine/src/jarvis_engine/learning/engine.py"
      via: "cmd.route forwarded to learn_from_interaction(route=)"
      pattern: "route=cmd\\.route"
    - from: "engine/src/jarvis_engine/learning/engine.py"
      to: "ResponseFeedbackTracker.record_feedback"
      via: "route kwarg"
      pattern: "record_feedback.*route=route"
---

<objective>
Fix the critical data quality bug (LEARN-04) and expose learning trackers on the bus.

Purpose: All learning data is currently stored with empty route/topic strings, making route quality scoring, usage prediction, and per-route dashboard metrics useless. This plan fixes the root cause by adding route/topic fields to LearnInteractionCommand, wiring them through the handler and engine to the trackers, updating all 4 dispatch sites in main.py to pass real route data, and exposing trackers on the bus so later plans can read from them.

Output: LearnInteractionCommand carries route and topic, all dispatch sites pass real data, trackers accessible via bus attributes.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-learning-system-activation/02-RESEARCH.md
@engine/src/jarvis_engine/commands/learning_commands.py
@engine/src/jarvis_engine/learning/engine.py
@engine/src/jarvis_engine/handlers/learning_handlers.py
@engine/src/jarvis_engine/app.py
@engine/src/jarvis_engine/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route/topic fields and wire through CQRS pipeline</name>
  <files>
    engine/src/jarvis_engine/commands/learning_commands.py
    engine/src/jarvis_engine/handlers/learning_handlers.py
    engine/src/jarvis_engine/learning/engine.py
    engine/src/jarvis_engine/app.py
  </files>
  <action>
1. In `engine/src/jarvis_engine/commands/learning_commands.py`, add two new fields to `LearnInteractionCommand` AFTER the existing `task_id` field (order matters for backward compat since callers use keyword args):
   ```python
   route: str = ""      # IntentClassifier route name (e.g., "routine", "complex", "math_logic")
   topic: str = ""      # Topic hint for usage pattern mining (first 100 chars of query)
   ```

2. In `engine/src/jarvis_engine/handlers/learning_handlers.py`, update `LearnInteractionHandler.handle()` to pass `route` and `topic` from the command to `learn_from_interaction()`:
   ```python
   result = self._learning_engine.learn_from_interaction(
       user_message=cmd.user_message,
       assistant_response=cmd.assistant_response,
       task_id=cmd.task_id,
       route=cmd.route,
       topic=cmd.topic,
   )
   ```

3. In `engine/src/jarvis_engine/learning/engine.py`, update `learn_from_interaction()` signature to accept `route: str = ""` and `topic: str = ""` parameters. Then pass them through:
   - Line 115 area: Change `self._feedback_tracker.record_feedback(user_message)` to `self._feedback_tracker.record_feedback(user_message, route=route)`
   - Line 122 area: Change `self._usage_tracker.record_interaction()` to `self._usage_tracker.record_interaction(route=route, topic=topic)`

4. In `engine/src/jarvis_engine/app.py`, after line 558 (the `bus._gateway = gateway` line), add bus exposure for all 3 trackers. This must be INSIDE the learning try block (after line 414 where learning_engine is created), NOT after line 558. Move the exposure to right after the learning_engine is created and handlers registered, but before the except block:
   ```python
   bus._pref_tracker = pref_tracker        # type: ignore[attr-defined]
   bus._feedback_tracker = feedback_tracker  # type: ignore[attr-defined]
   bus._usage_tracker = usage_tracker        # type: ignore[attr-defined]
   ```
   Place these 3 lines at approximately line 425 (after the FlagExpiredFactsHandler registration, before the except block at line 430). This way they are only set when learning initialization succeeds.
  </action>
  <verify>
    Run `python -m pytest engine/tests/ -x -q --timeout=120` and confirm all existing tests pass. No test should break because all new fields have defaults and all callers use keyword arguments.
  </verify>
  <done>LearnInteractionCommand has route/topic fields. Handler forwards them. Engine passes route to record_feedback() and route/topic to record_interaction(). Trackers exposed on bus. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 4 dispatch sites in main.py to pass route data</name>
  <files>
    engine/src/jarvis_engine/main.py
  </files>
  <action>
Update all 4 `LearnInteractionCommand` dispatch sites in `main.py` to pass route and topic:

**Site 1: cmd_learn() (line ~2048)**
This is the manual CLI learning command. No classification happens here.
- Add `route="manual"` (it is a manual learning invocation, not classified).
- Add `topic=user_message[:100]` for basic topic tracking.

**Site 2: _web_augmented_llm_conversation() (line ~3101)**
The `_route` variable is available (defined at line 3003 as "web_research" default, or classified).
- Add `route=_route,` to the LearnInteractionCommand constructor.
- Add `topic=text[:100],` to capture the query as topic hint.

**Site 3: cmd_voice_run() LLM conversation path (line ~3968)**
The `_route` variable is available (defined at line 3839 as "routine" default, or classified).
- Add `route=_route,` to the LearnInteractionCommand constructor.
- Add `topic=text[:100],` to capture the query as topic hint.

**Site 4: cmd_voice_run() non-LLM command learning (line ~4037)**
The `intent` variable is available (e.g., "brain_context", "mission_status").
- Add `route=intent,` to the LearnInteractionCommand constructor (intent IS the route for commands).
- Add `topic=text[:100],` to capture the command text as topic.
  </action>
  <verify>
    Run `python -m pytest engine/tests/test_main.py -x -q --timeout=120` to verify main.py CLI tests pass. Then run full suite: `python -m pytest engine/tests/ -x -q --timeout=120`.
  </verify>
  <done>All 4 dispatch sites pass real route and topic values. Route comes from _route (classified) or intent (command name). Topic is query text truncated to 100 chars.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for route/topic propagation and bus exposure</name>
  <files>
    engine/tests/test_learning_route_params.py
  </files>
  <action>
Create a new test file `engine/tests/test_learning_route_params.py` with the following tests:

1. **test_learn_interaction_command_has_route_topic**: Verify LearnInteractionCommand can be constructed with route and topic kwargs, and that defaults are empty strings.

2. **test_learn_interaction_command_backward_compat**: Verify constructing LearnInteractionCommand WITHOUT route/topic still works (backward compatibility).

3. **test_handler_passes_route_topic**: Mock learning_engine.learn_from_interaction, dispatch a LearnInteractionCommand with route="complex" and topic="test query", verify learn_from_interaction was called with route="complex" and topic="test query".

4. **test_engine_passes_route_to_feedback_tracker**: Create a ConversationLearningEngine with a mock feedback_tracker and mock usage_tracker. Call learn_from_interaction(user_message="great job", assistant_response="thanks", route="routine", topic="greeting"). Verify feedback_tracker.record_feedback was called with route="routine". Verify usage_tracker.record_interaction was called with route="routine" and topic="greeting".

5. **test_engine_backward_compat_no_route**: Call learn_from_interaction WITHOUT route/topic. Verify it still works (feedback tracker called with route="" default, usage tracker called with route="" and topic="").

6. **test_bus_exposes_trackers**: Use create_bus() from app.py with a temporary directory that has a valid memory DB. Verify bus._pref_tracker, bus._feedback_tracker, and bus._usage_tracker are set and are instances of their respective classes. Use `getattr(bus, "_pref_tracker", None)` pattern.

For test_bus_exposes_trackers, create a minimal test that:
- Creates a temp directory with the correct structure
- Calls create_bus()
- Checks tracker attributes exist
- This test may need to be skipped if create_bus() requires complex setup -- in that case, just test the tracker attribute pattern with a mock bus.
  </action>
  <verify>
    Run `python -m pytest engine/tests/test_learning_route_params.py -x -v` and confirm all tests pass. Then run full suite to confirm no regressions: `python -m pytest engine/tests/ -x -q --timeout=120`.
  </verify>
  <done>6 tests covering route/topic field existence, backward compatibility, handler forwarding, engine forwarding to trackers, and bus exposure. All pass alongside existing test suite.</done>
</task>

</tasks>

<verification>
1. `python -m pytest engine/tests/ -x -q --timeout=120` -- all tests pass (existing + new)
2. `grep -n "route=" engine/src/jarvis_engine/main.py | grep LearnInteractionCommand` -- confirms all 4 sites pass route
3. `grep -n "bus._pref_tracker" engine/src/jarvis_engine/app.py` -- confirms tracker exposure
4. `grep -n "route=route" engine/src/jarvis_engine/learning/engine.py` -- confirms engine forwards route
5. `python -c "from jarvis_engine.commands.learning_commands import LearnInteractionCommand; c = LearnInteractionCommand(route='test', topic='test'); print(c.route, c.topic)"` -- prints "test test"
</verification>

<success_criteria>
- LearnInteractionCommand has route and topic fields with empty string defaults
- Handler forwards cmd.route and cmd.topic to learn_from_interaction()
- Engine passes route to record_feedback() and route+topic to record_interaction()
- All 4 dispatch sites in main.py pass real route data (not empty strings)
- Trackers exposed on bus as _pref_tracker, _feedback_tracker, _usage_tracker
- All existing tests pass (no breaking changes)
- 6 new tests pass covering the complete chain
</success_criteria>

<output>
After completion, create `.planning/phases/02-learning-system-activation/02-01-SUMMARY.md`
</output>
