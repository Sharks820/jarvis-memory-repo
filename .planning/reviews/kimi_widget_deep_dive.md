- Replace heavy animation assets with transform/opacity micro-animations.
- Batch UI animation updates and pause non-visible animations.
- Autofocus command input on open and keep keyboard-only command path first-class.
- Pre-fill command context from current app/window and clipboard with opt-in controls.
- Use local VAD plus lightweight wake-word model for low-power always-on listening.
- Fall back automatically to push-to-talk on constrained CPUs.
- Add memory checkpoints around long-running operations with automatic rollback.
- Enforce memory usage thresholds and compact/rotate context before overflow.
- Keep append-only signed mutation logs for forensic replay and integrity checks.
- Verify model/plugin signatures before loading local runtimes.
- Encrypt local config/state stores with per-device keys.
- Sanitize all rendered content and enforce strict CSP-equivalent local policies.
- Keep immutable local audit trails for security-sensitive actions.
- Force read-only safe mode when integrity/security checks fail.
- Pin dependencies and run routine vulnerability scans for supply-chain hardening.
