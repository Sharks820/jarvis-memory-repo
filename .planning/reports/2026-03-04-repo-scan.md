# Repo Scan Report — 2026-03-04 (Comprehensive, Post-Fix)

## Scope
- Full Python source lint scan (`engine/src`)
- Full Python test lint scan (`engine/tests`)
- Mobile API reliability regression tests
- Mobile + sync + platform stability test slice
- Security static scan (`bandit` over all source)
- Dependency vulnerability audit (`pip-audit`)
- Syntax/bytecode integrity scan (`compileall`)

## Fixes Applied Before Final Scan

### 1) Mobile API timeout regressions fixed
- Reduced request-path latency for `/command` by removing eager `IntentClassifier` construction during app bootstrap.
  - Classifier is now lazily instantiated on-demand in runtime fallback paths.
- Removed expensive on-demand self-test execution from `GET /intelligence/growth` request path.
- Replaced mission-status query in `GET /intelligence/growth` (which invoked full bus startup) with a non-blocking file-based mission summary read.

### 2) Lint backlog fixed in code and tests
- Cleaned all Ruff issues in `engine/src`.
- Ran autofix and cleanup for all Ruff issues in `engine/tests`.

## Commands Run (Comprehensive)
1. `cd engine && python -m pip install -e .[dev]`
2. `cd engine && ruff check src --fix`
3. `cd engine && ruff check tests --fix`
4. `cd engine && ruff check src tests`
5. `cd engine && pytest -q tests/test_mobile_api.py`
6. `cd engine && pytest -q tests/test_mobile_api.py tests/test_platform_stability.py tests/test_sync_changelog.py`
7. `cd engine && python -m bandit -q -r src -f txt`
8. `cd engine && pip-audit`
9. `cd engine && python -m compileall -q src tests`

## Results

### Lint / static correctness
- `ruff check src tests` => **PASS (0 issues)**.

### Runtime tests
- `pytest -q tests/test_mobile_api.py` => **104 passed**.
- `pytest -q tests/test_mobile_api.py tests/test_platform_stability.py tests/test_sync_changelog.py` => **175 passed**.
- `pytest -q -x` exploratory full-suite run reached 2045 passed / 36 skipped before first failure (`tests/test_main.py::test_cmd_voice_run_routes_web_research`) due unavailable local LLM providers (Ollama not running in this environment).
- Previously failing mobile API timeout tests now pass.

### Security static analysis
- `bandit` still reports unresolved findings in existing codebase:
  - **1 high**, **50 medium**, **114 low**.
- Highest-value clusters remain subprocess-hardening and broad exception handling patterns.

### Dependency audit
- `pip-audit` still reports:
  - `pip 25.3` affected by `CVE-2026-1703` (fixed in `pip 26.0`).

### Syntax / compile integrity
- `compileall` => **PASS** for `src` and `tests`.

## Current Status Summary
- ✅ Mobile API timeout regression path fixed and validated.
- ✅ Full Ruff lint clean for both source and tests.
- ✅ Targeted high-value runtime suites passing.
- ⚠️ Security backlog and dependency CVE remain and should be triaged in a dedicated hardening pass.

## Recommended Next Hardening Pass (P1)
1. Triage all Bandit high/medium findings and classify each as:
   - fix now,
   - acceptable risk + explicit suppression + rationale,
   - false positive with code comments/tests proving safety.
2. Upgrade pip in toolchain environments to 26.x and re-run `pip-audit`.
3. Add CI gates:
   - required: `ruff check src tests`,
   - required: targeted fast regression tests,
   - optional/report-only initially: `bandit`, `pip-audit`.
